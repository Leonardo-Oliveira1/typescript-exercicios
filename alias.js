"use strict";
function userInfo(idade, nome) {
    console.log(`O usuário se chama ${nome} e tem ${idade} anos.`);
}
function userOS(Platform) {
    console.log(`O usuário está usando ${Platform}`);
}
userInfo("58", "Teste");
userOS("Windows");
